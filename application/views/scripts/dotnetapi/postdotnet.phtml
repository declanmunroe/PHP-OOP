<a class="dotnet" data-id="50">Send GET REQUEST to dot net</a>
<br>
<br>
<a class="dotnetpostfamily" data-firstname="Annette" data-lastname="Munroe" data-typeofmember="Mother">Send POST REQUEST Update Family Table</a>
<br>
<br>
<a class="dotnetupdate" data-id="1002" data-firstname="Michael" data-lastname="Sweeney" data-typeofmember="Uncle">Send UPDATE REQUEST Update Family Table</a>
<br>
<br>
<a class="dotnetdelete">Send DELETE REQUEST Remove Record in Family Table</a>

<script>
    $(".dotnet").click(function () {
        var num = $(this).attr("data-id");
        //alert(num);

        $.ajax({
            type: 'GET',
            url: 'http://localhost:53553/api/values',
//            data: { num: num },
            success: function (result) {
                //alert("It worked");
                console.log(result);
            }
        });
    });
    
    $(".dotnetpostfamily").click(function () {
        var family = {};
        family.First_Name = $(this).attr("data-firstname");
        family.Last_Name = $(this).attr("data-lastname");
        family.Type_Of_Member = $(this).attr("data-typeofmember");
        //alert(number);
        
        $.ajax({
            type: 'POST',
            url: 'http://localhost:53553/api/values',
            data: JSON.stringify(family), // Need to use stringify and set the content type to json as im sending data through the body and not an object
            contentType: "application/json; charset=utf-8",
            success: function (result) {
                console.log(result);
            }
        });
    });
    
    $(".dotnetupdate").click(function () {
        var id = $(this).attr("data-id");
        
        var family = {};
        family.First_Name = $(this).attr("data-firstname");
        family.Last_Name = $(this).attr("data-lastname");
        family.Type_Of_Member = $(this).attr("data-typeofmember");
        //alert(number);
        
        $.ajax({
            type: 'PUT',
            url: 'http://localhost:53553/api/values/' + id,
            data: JSON.stringify(family), // Need to use stringify and set the content type to json as im sending data through the body and not an object
            contentType: "application/json; charset=utf-8",
            success: function (result) {
                console.log(result);
            }
        });
    });
    
    $(".dotnetdelete").click(function () {
        
        $.ajax({
            type: 'DELETE',
            url: 'http://localhost:53553/api/values/' + 2002,
            success: function (result) {
                console.log(result);
            }
        });
    });
</script>